import type { Region } from "@/lib/types";

/** Monaco est une commune unique sans subdivision administrative interne : ses quartiers (Monaco-Ville, Monte-Carlo, La Condamine, Fontvieille...) n'ont pas de statut ni de gouvernement propres. */
export const regions: Region[] = [];
